from setuptools import setup, find_packages

setup(
    name="Register",
    version="1.0.0",
    author="Daniel Oraki",
    description="A simple register form using user's age",
    packages=find_packages()
)